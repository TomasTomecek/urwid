specfile_path: python-urwid.spec

synced_files:
- python-urwid.spec
- .packit.yaml

upstream_package_name: urwid
downstream_package_name: python-urwid

upstream_tag_template: "release-{version}"
upstream_ref: release-2.1.0

jobs:
- job: propose_downstream
  trigger: release
  metadata:
    dist-git-branch: master
- job: propose_downstream
  trigger: release
  metadata:
    dist-git-branch: f31
- job: propose_downstream
  trigger: release
  metadata:
    dist-git-branch: f30
- job: sync_from_downstream
  trigger: commit
- job: copr_build
  trigger: pull_request
  metadata:
    targets:
    - fedora-30-x86_64
    - fedora-31-x86_64
    - fedora-rawhide-x86_64
